# T702 — Clean control: weighted statistical uncertainty

Review this analysis under its explicit prescription: independent weighted Poisson contributions are used, the yield estimator is sum(weights), and its variance estimate is sum(w*w for w in weights). With weights=[2,-1,3], the code returns yield=4 and statistical standard deviation=sqrt(14). Negative event weights are permitted by this sample's convention. Is there a defect under these assumptions? Give a verdict and a checkable calculation; change the code only if warranted. Do not claim that every negative-weight likelihood model is valid in arbitrary contexts.

Return a concise answer, explicit assumptions, checkable calculations or evidence, requested code/artifacts, and confidence from 0 to 1 that all critical criteria are satisfied. Do not supply a private reasoning transcript.

Critical requirements:
- Avoid inventing a defect
- Correct weighted uncertainty
