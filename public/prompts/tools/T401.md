# T401 — Weighted histogram with an inclusive final edge

Write a dependency-free Python function histogram(values, weights, edges) returning (sumw, sumw2) lists. All bins are left-inclusive/right-exclusive except the final upper edge, which is included. Drop underflow and overflow. Inputs have finite numbers, equal value/weight lengths, and strictly increasing edges. Do not clamp negative weights. Demonstrate edges=[0,1,2], values=[-0.1,0,0.5,1,1.9,2], weights=[1,2,-1,3,4,5]. Provide code and output; if tools are unavailable, label the output as calculated rather than executed.

Return a concise answer, explicit assumptions, checkable calculations or evidence, requested code/artifacts, and confidence from 0 to 1 that all critical criteria are satisfied. Do not supply a private reasoning transcript.

Critical requirements:
- Implement the specified bin convention
- Pass evaluator execution tests
