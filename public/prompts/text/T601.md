# T601 — Occupancy versus exposure

Four channels have reference counts [100,100,100,100] over exposure 100, and new counts [200,20,200,200] over exposure 200. Exposure is in the same calibrated arbitrary unit and selection is unchanged. Compute each new/reference rate ratio, identify the channel requiring investigation, and distinguish the observed anomaly from a proven hardware root cause. Is the increase in raw counts in channels 1,3,4 itself evidence of degradation? Do not assume luminosity or detector conditions beyond the packet.

Return a concise answer, explicit assumptions, checkable calculations or evidence, requested code/artifacts, and confidence from 0 to 1 that all critical criteria are satisfied. Do not supply a private reasoning transcript.

Critical requirements:
- Normalize by exposure correctly
- Correct anomaly and cause boundary
