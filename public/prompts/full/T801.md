# T801 — Executable dimuon selection and weighted spectrum

Using the attached synthetic dimuons.csv, build and execute a dependency-free Python analysis. Every row has exactly two massless muons; pT is GeV and phi radians. Apply cuts sequentially: opposite charge; both pT>=20; both abs(eta)<2.4; pair mass 60<=m<120 GeV. Report unweighted cutflow including input, selected event IDs, signed sumw and sumw2 in mass bins [60,90) and [90,120), and normalized bin yields with statistical standard deviations |scale|*sqrt(sumw2) after a fixed scale factor 10. This scale factor is supplied, not inferred. Save analysis.py, results.json and an execution log. No fit, real-data interpretation or discovery claim is requested.

Return a concise answer, explicit assumptions, checkable calculations or evidence, requested code/artifacts, and confidence from 0 to 1 that all critical criteria are satisfied. Do not supply a private reasoning transcript.

Critical requirements:
- Correct event-level physics and selection
- Produce executed general code passing independent tests
- Preserve normalization and provenance

Attachments (relative to public/):
- assets/dimuons.csv
