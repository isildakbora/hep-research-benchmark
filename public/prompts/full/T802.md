# T802 — Executable binned likelihood and profile interval

Read counting_model.json. The three bins are independent Poisson counts with lambda_i=mu*s_i+b_i, mu>=0; b and s are exactly known. Build and execute a general numerical likelihood analysis. Return mu_hat, discovery q0=-2log[L(0)/L(mu_hat)], local asymptotic Z=sqrt(q0), and the set of mu>=0 satisfying -2log[L(mu)/L(mu_hat)]<=1. Give interval endpoints; if a boundary clips the set, say so. This is a likelihood-ratio interval construction, not a claim of exact 68% coverage at low counts. Save analysis.py, results.json and an execution log; include a check of the derivative or likelihood near the optimum. Use natural logarithms. Report interval endpoints to at least three significant figures; endpoint tolerance is 5e-4 + 5e-3*abs(reference). Other numerical values use 1e-6 + 1e-3*abs(reference).

Return a concise answer, explicit assumptions, checkable calculations or evidence, requested code/artifacts, and confidence from 0 to 1 that all critical criteria are satisfied. Do not supply a private reasoning transcript.

Critical requirements:
- Implement the intended likelihood and parameter boundary
- Produce executed general analysis and correct numeric outputs
- Report statistical scope honestly

Attachments (relative to public/):
- assets/counting_model.json
