# T601 — Occupancy versus exposure

Read the reference and new counts from the attached occupancy plot. Exposure uses the same calibrated arbitrary unit and selection is unchanged. Compute each new/reference rate ratio, identify the channel requiring investigation, and distinguish the observed anomaly from a proven hardware root cause. Are increased raw counts alone evidence of degradation? Do not assume luminosity or detector conditions beyond the packet.

Return a concise answer, explicit assumptions, checkable calculations or evidence, requested code/artifacts, and confidence from 0 to 1 that all critical criteria are satisfied. Do not supply a private reasoning transcript.

Critical requirements:
- Normalize by exposure correctly
- Correct anomaly and cause boundary

Attachments (relative to public/):
- assets/occupancy.png
